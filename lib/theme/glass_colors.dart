import 'package:flutter/material.dart';

/// Palette for the "high-trust premium" glassmorphism design system:
/// deep charcoal surfaces with subtle glowing borders.
class GlassColors {
  GlassColors._();

  static const background = Color(0xFF0B0E14);
  static const surface = Color(0xFF12161F);
  static const glowBorder = Color(0xFF2A3A5A);
  static const glowBorderHover = Color(0xFF4C6EA8);
  static const glassFill = Color(0x1AFFFFFF);
  static const glassFillHover = Color(0x26FFFFFF);
}
